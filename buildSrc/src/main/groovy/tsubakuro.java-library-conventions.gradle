plugins {
    id 'java-library'
    id 'maven-publish'

    id 'tsubakuro.java-conventions'
}

publishing {
    repositories {
        maven {
            name 'GitHubPackages'
            url 'https://maven.pkg.github.com/project-tsurugi/tsubakuro'
            credentials {
                username findProperty('gpr.user') ?: System.getenv('GPR_USER')
                password findProperty('gpr.key') ?: System.getenv('GPR_KEY')
            }
        }
    }
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }
}
