plugins {
    id 'java-library'
    id 'maven-publish'

    id 'tsubakuro.java-conventions'
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            artifactId = project.name

            from components.java

            artifact sourcesJar {
                classifier = 'sources'
                extension  = 'jar'
            }
        }
    }
}
