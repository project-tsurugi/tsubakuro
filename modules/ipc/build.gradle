plugins {
    id 'tsubakuro.java-library-conventions'
    id 'tsubakuro.jni-library-conventions'
}

dependencies {
    implementation project(':tsubakuro-common')
}

tasks.withType(Test) {
    environment "LD_PRELOAD", System.getenv()['JAVA_HOME'] + '/lib/server/libjsig.so'
}
